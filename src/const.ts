export enum AppRoute {
  Root = '/'
}

export enum NameSpace {
  tasks = 'TASKS',
}

export const FilterItems = ['All', 'Active', 'Completed'];

export const FilterType = {
  ALL: 'All',
  ACTIVE: 'Active',
  COMPLETED: 'Completed',
};

export const DEFAULT_FILTER = 'All';
